const drink = {
  color: 'brown',
  carbonated: true,
  sugar: 40,
};

// Type Alias
type Drink = [string, boolean, number];

// const cola: Drink = ['brown', true, 40];
const sprite: Drink = ['brown', true, 40];
const coffee: Drink = ['brown', true, 0];
